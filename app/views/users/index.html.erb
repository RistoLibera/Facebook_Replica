<!-- Actually, this is friend request page -->
<% content_for(:title, "Requests") %>
<div class="is-flex is-justify-content-space-around">

  <div>
    <p>Fake interaction</p>
  </div>
  <div>
    <h1>People You May Know</h1>
    <!-- Friend request -->
    <div>
      <%= render partial: "requests/all_requests", locals: { users: @user } %>
    </div>
    <!-- Other User -->
    <div>
      <%= render partial: "all_users", locals: { users: @user } %>    
    </div>
  </div>
</div>